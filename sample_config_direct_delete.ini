[general]
aws_profile = default
log_directory = /opt/logs/archival

[secrets]
database_secret = prince_test_rds_partition/db-secret
monitoring_db_secret = prince_test_rds_partition/monitoring-db-secret

[s3]
bucket_name = my-data-bucket
file_path = exports/{date}/old_data/

[database]
table_name = old_user_sessions
host = prod-db-host.example.com
database = production_db
batch_size = 5000
health_check_interval = 3

# SQL queries (use {} as placeholder for ID list)
# DIRECT DELETE MODE: Only delete query present - no archival
# Note: archival_insert_query is commented out or not present
# archival_insert_query = 

delete_query = DELETE FROM testing.old_user_sessions WHERE id IN ({})

[replica]
host1 = replica1.example.com
host2 = replica2.example.com